pipeline {
    agent none 

    parameters {
		string(name: 'REPOURL', description: 'SCM repository url address')
		string(name: 'BRANCH', description: 'branch of repository')
    }

    stages {
        stage ('dependencyCheck') {
            agent any

            steps {
                dependencyCheck additionalArguments: '', odcInstallation: 'dependencycjeck'
            }
        }

   }
}
